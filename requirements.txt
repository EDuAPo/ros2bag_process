# 科学计算和地理空间
numpy==1.26.4
pyproj==3.7.1
scipy==1.15.3

# 计算机视觉和进度条
opencv-python==4.10.0.84
tqdm==4.67.1

# ROS Bag工具依赖
rosbags==0.10.0
lz4==3.1.3
ruamel.yaml==0.18.16
zstandard==0.25.0

# GStreamer/GUI 依赖
pycairo==1.20.1
PyGObject==3.42.1

# 点云处理
open3d==0.19.0

# 配置文件解析
PyYAML==6.0.3

# 注意事项：
# 1. PyGObject 和 pycairo 需要先安装系统依赖才能成功编译
#    sudo apt install -y libgirepository1.0-dev libcairo2-dev pkg-config python3-dev
# 2. 如果使用虚拟环境，建议使用 --system-site-packages 选项以使用系统的 GStreamer 绑定
# 3. open3d 包较大（约500MB），安装可能需要几分钟
